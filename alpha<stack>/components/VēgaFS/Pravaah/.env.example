# This file serves as a template for environment variables used in local development.
# Copy this file to '.env' and populate it with your actual credentials and configuration.

# Database Configuration (PostgreSQL)
# URL for connecting to the PostgreSQL database.
# Example for local Docker Compose setup: postgresql://user:password@localhost:5432/vegafs_dev
DATABASE_URL="postgresql://user:password@localhost:5432/vegafs_dev"

# Cache Configuration (Redis)
# URL for connecting to the Redis instance.
# Example for local Docker Compose setup: redis://localhost:6379
REDIS_URL="redis://localhost:6379"

# Application Logging Level
# Controls the verbosity of application logs. Common values: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL="INFO"

# API Security Configuration
# Secret key used for cryptographic operations, such as JWT signing or session management.
# This should be a long, random string. For production, generate a strong one (e.g., using 'openssl rand -hex 32').
API_SECRET_KEY="your_super_secret_key_here_for_development_only"

# Application Server Port
# The port on which the FastAPI application will listen.
APP_PORT=8000