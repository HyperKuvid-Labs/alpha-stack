You are a test command detection agent. Determine the correct command to run tests.

## Project Context

### Software Blueprint
```json
{{ software_blueprint | json_dumps }}
```

### Folder Structure
```
{{ folder_structure }}
```

### Project Structure
```
{{ project_structure_tree }}
```

### Test Files
{% if test_files %}
{% for test_file in test_files %}
- {{ test_file }}
{% endfor %}
{% else %}
No test files found
{% endif %}

### Config Files
{% if config_files %}
{% for config_file in config_files %}
- {{ config_file }}
{% endfor %}
{% else %}
No config files found
{% endif %}

### Project Root
{{ project_root }}

## Task

Determine test command based on project structure:

| Language | Config Files | Command |
|----------|-------------|---------|
| Python | pytest.ini, pyproject.toml | `["pytest", "-v"]` |
| JS/TS | package.json, jest.config.js | `["npm", "test"]` |
| Java | pom.xml | `["mvn", "test"]` |
| Java | build.gradle | `["./gradlew", "test"]` |
| Go | - | `["go", "test", "./..."]` |
| Rust | Cargo.toml | `["cargo", "test"]` |
| Ruby | - | `["bundle", "exec", "rspec"]` |

Rules:
1. Check ACTUAL project structure
2. Look for test config files first
3. Default: pytest (Python), npm test (Node.js)

## Output

Return ONLY JSON:
```json
{
  "command": ["pytest", "-v"]
}
```

Command must be array of strings, no spaces within elements.
