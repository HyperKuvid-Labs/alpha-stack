Generate a production-ready Dockerfile for this project.

## Project Context

### Software Blueprint
```json
{{ software_blueprint | json_dumps }}
```

### Folder Structure
```
{{ folder_structure }}
```

### File Output Format
```json
{{ file_output_format | json_dumps }}
```

### Project Root
{{ project_root }}

### File Summaries
{% if file_summaries %}
{% for file_path, description in file_summaries.items() %}
- **{{ file_path }}**: {{ description }}
{% endfor %}
{% else %}
No file summaries available
{% endif %}

### External Dependencies
{% if external_dependencies %}
{% for dep in external_dependencies %}
- {{ dep.get("raw", "") }}
{% endfor %}
{% else %}
No external dependencies
{% endif %}

## Constraints

1. **No linting/formatting tools** - Even if in dependency files, exclude code quality tools
2. **Module paths** - Configure properly for `src/` structure before imports
3. **DO NOT run tests during build** - Tests will be executed separately via `docker run` after the image is built. Do not include any `RUN pytest`, `RUN npm test`, `RUN bun test`, or similar test commands.
4. Rust image tags - Use correct format: rust:slim-bullseye or rust:slim-bookworm, NOT rust:bullseye-slim (which doesn't exist and causes "not found" errors)

## Requirements

1. **Base image**: Match tech stack from blueprint
2. **Dependencies**: Install using appropriate package manager (skip linting tools)
3. **Working directory**: Set up properly
4. **Copy files**: Setup files first, then code
5. **Syntax validation**: Validate after copying files (e.g., `python -m compileall` or TypeScript build)
6. **Build commands**: Include compile/transpile steps if needed
7. **Expose ports**: For web services
8. **Entry point**: CMD/ENTRYPOINT to run the application
9. **Non-root user**: Create and use (e.g., `useradd -m -u 1000 appuser`)
10. **Multi-stage builds**: Use if beneficial for the tech stack
11. **Health checks**: For long-running services
12. **BuildKit cache mounts**: Use cache mounts for package managers to speed up rebuilds:
    - Python: `RUN --mount=type=cache,target=/root/.cache/pip pip install -r requirements.txt`
    - Node/npm: `RUN --mount=type=cache,target=/root/.npm npm ci`
    - Node/bun: `RUN --mount=type=cache,target=/root/.bun bun install`
    - Go: `RUN --mount=type=cache,target=/go/pkg/mod go build`
    - Rust: `RUN --mount=type=cache,target=/usr/local/cargo/registry cargo build`

## Output

Return ONLY raw Dockerfile content, no markdown or explanations.
