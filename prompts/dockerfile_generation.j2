Generate a production-ready Dockerfile for the project based on the following context:

## Project Context

**IMPORTANT**: Projects are generated inside `/Users/adityagk/Desktop/project-1/created_projects` directory. The project root path is relative to this base directory.

### Software Blueprint
```json
{{ software_blueprint | json_dumps }}
```

### Folder Structure
```
{{ folder_structure }}
```

### File Output Format
```json
{{ file_output_format | json_dumps }}
```

### Project Root
{{ project_root }}
**Note**: This is the absolute path to the project root directory.

## Requirements

1. **Base Image**: Choose appropriate base image based on the tech stack (Python, Node.js, Java, etc.)
2. **Dependencies**: Install all required dependencies from package managers (pip, npm, etc.)
3. **Working Directory**: Set up appropriate working directory structure
4. **Copy Files**: Copy project files to container
5. **Build Commands**: Include any build steps needed (compile, transpile, etc.)
6. **Syntax Validation**: AFTER copying files, add a step to validate syntax:
   - For Python: `RUN python3 -m py_compile main.py src/**/*.py tests/**/*.py || find . -name "*.py" -not -path "./.git/*" -not -path "./__pycache__/*" -exec python3 -m py_compile {} \;`
   - For JavaScript/TypeScript: `RUN npm run build` or `tsc --noEmit` if available
   - This ensures syntax errors are caught during build
7. **Expose Ports**: Expose necessary ports for the application
8. **Entry Point**: Set up appropriate CMD or ENTRYPOINT to run the application
9. **Multi-stage Build**: Use multi-stage builds if beneficial for optimization
10. **Health Checks**: Include HEALTHCHECK if applicable
11. **Environment Variables**: Set up necessary environment variables
12. **Non-root User**: Create and use a non-root user for security (e.g., `RUN useradd -m -u 1000 appuser && USER appuser`)
13. **Test Command**: Default CMD should run tests (e.g., `CMD ["pytest"]` for Python projects)

## Output Format

Return ONLY the Dockerfile content without any markdown formatting, code blocks, or explanations.
Just the raw Dockerfile content that can be directly used.
