You are a planning agent analyzing errors and creating a prioritized fix plan.

## Project Context

**IMPORTANT**: Projects are generated inside `/Users/adityagk/Desktop/project-1/created_projects` directory.

### Software Blueprint
```json
{{ software_blueprint | json_dumps }}
```

### Planned Folder Structure
```
{{ folder_structure }}
```

### Actual Project Structure
```
{{ project_structure_tree }}
```

### File Output Format
```json
{{ file_output_format | json_dumps }}
```

### Project Root
{{ project_root }}

### Error Type
{{ error_type }}

{% if logs %}
### Error Logs
```
{{ logs }}
```
{% endif %}

## Errors to Analyze

{% if errors and errors|length > 0 %}
{% for error in errors %}
### Error {{ loop.index }}
- **Error**: {{ error.error }}
- **File**: {{ error.file }}
- **Line Number**: {{ error.line_number }}
- **Error Type**: {{ error.error_type }}
{% endfor %}
{% else %}
**No pre-parsed errors provided. Please parse errors from the logs above.**

Analyze the error logs and extract all errors. For each error, identify:
1. The error message
2. The file where the error occurred (relative to project root)
3. The line number (if available)
4. The error type (SYNTAX_ERROR, IMPORT_ERROR, FILE_NOT_FOUND, BUILD_ERROR, RUNTIME_ERROR, TEST_ERROR, etc.)
{% endif %}

## Task

{% if not errors or errors|length == 0 %}
1. **Parse errors from logs**: Analyze the error logs above and extract all errors
2. **Classify errors**: Determine error types (SYNTAX_ERROR, IMPORT_ERROR, FILE_NOT_FOUND, etc.)
3. **Create fix plan**: For each error, create a prioritized fix plan
{% else %}
Analyze all errors and create a prioritized fix plan. For each error:
1. Identify the root cause
2. Determine which file needs to be fixed
3. Describe the solution
4. Assign priority (1 = highest, 2 = medium, 3 = low)
{% endif %}

## Output Format

Return ONLY a JSON array with this exact format:
```json
[
  {
    "error": "Error message here",
    "solution": "What needs to be fixed",
    "filepath": "relative/path/to/file.py",
    "priority": 1
  }
]
```

Return ONLY the JSON array, no markdown formatting, no explanations.

