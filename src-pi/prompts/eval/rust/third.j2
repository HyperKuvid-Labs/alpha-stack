Implement a generic BST BinaryTree<T> where T: Ord.

Operations:

insert(&mut self, value: T)

contains(&self, value: &T) -> bool

iter(&self) -> InOrderIter<T> (returns iterator over &T in sorted order).
Challenges:

Use Box<Node<T>> for heap allocation.

Handle lifetimes correctly for the iterator (iterator borrows the tree).

Implement recursive or iterative traversal using a stack in the iterator.
Add tests for:

Inserting duplicates (define behavior).

Iterating empty tree.

Collecting iterator into a Vec and verifying sorted order.